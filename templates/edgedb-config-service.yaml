apiVersion: v1
kind: Service
metadata:
   name: edgedb_config_service
   labels:
     app: edgedb_config_service
spec:
   ports:
     - port: 1337
       protocol: TCP
       targetPort: 1337
       protocol: "TCP"
   selector:
     app: pi-app
     backend: dbinit-service
