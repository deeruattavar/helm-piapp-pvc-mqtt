apiVersion: v1
kind: Service
metadata:
   name: edgedb-config-service
   labels:
     app: edgedb-config-service
spec:
   ports:
     - port: 1337
       protocol: TCP
       targetPort: 1337
   selector:
     app: pi-app
     type: dbconfig-ui 
     backend: dbinit-service
