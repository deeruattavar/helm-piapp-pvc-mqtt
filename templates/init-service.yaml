apiVersion: v1
kind: Service
metadata:
   name: edgedbconfigservice
   labels:
     app: edgedb_config_service
spec:
   ports:
     - port:80
       protocol: TCP
       targetPort: 4201
       protocol: "TCP"
       http: True
   clusterIP: 10.43.1.1   
   selector:
     app: pi-app
     backend: dbinit-service
